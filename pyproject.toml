[project]
name = "agentteam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
optional-dependencies = { dev = [
    "prek",
    "ruff",
    "ty",
], "test" = [
    "pytest",
    "pytest-xdist",
] }
